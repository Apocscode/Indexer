# Index Controller Firmware

Build artifacts, IDE settings, and dependencies — do not commit.

## Quick Commands

```bash
# Build
pio run

# Upload to ESP32
pio run -t upload

# Serial Monitor
pio device monitor

# Clean
pio run -t clean
```
